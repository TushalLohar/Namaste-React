# Namaste React

# Parcel (Bundler)- episode 2

- Dev build – optimized for development
- Local server – hosts app on localhost
- Auto refresh – updates browser automatically
- HMR (Hot Module Replacement) – instant UI update without full reload
- File watching algorithm – written in C++ (fast detection)
- Caching – faster rebuilds
- Image optimization – reduces image size
- Minification – removes extra spaces & code
- Bundling – combines multiple files
- Compression – reduces bundle size
- React is fast, but bundlers help a lot – performance boost
- Parcel uses node_modules internally – manages dependencies
- Code splitting – loads code when required
- Differential bundling – supports older browsers
- Diagnostics – better error messages
- Error handling – helpful suggestions
- HTTP & HTTPS support – secure hosting
- Tree shaking – removes unused code
- Different dev & production builds – optimized output
- Uses browserslist – browser compatibility support


# react hooks
- normal JS utilities functions - written by facebook
- presnt in node moudles
- useState() - superpowerful state varibales in react
- useEffect() - 
